<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.optimus.mapper.AddressMapper">
<select id="query" resultType="com.optimus.dto.results.AddressResultDTO">
    <if test="currentId == null and parentId ==null and level==null">
        select * from area_info where status=0 and level=1
    </if>
    <if test="currentId !=null">
        select * from area_info where status=0 and parent_id=#{currentId}
    </if>
    <if test="parentId !=null">
        select * from area_info where status=0 and parent_id=#{parentId}
    </if>
</select>


</mapper>